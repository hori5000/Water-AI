---
doc_id: WATER-AI-PROJECT-DEF-001
title: 파이브텍 정수·하수 AI 프로젝트 성격·난이도 및 현재 단계
project: RS-2025-02307821
category: 프로젝트정의
status: active
revision: 1.0
last_updated: 2026-09-09
---

# 파이브텍 정수·하수 AI 프로젝트 성격·난이도 및 현재 단계

## 1. 프로젝트 한 줄 정의

이 과제에서 파이브텍 외주 AI 개발 관점의 핵심은 **단순한 AI 예측모델 1개 개발이 아니라, 현장 OT 구조를 해석하고 실제 운전데이터를 확보·정합화한 뒤 예측·전력·최적화 모델을 SCADA/PLC 안전계층과 연계하는 산업 OT·데이터·AI 통합 시스템 개발**이다.

> **중요:** 아래 난이도 평가는 연구계획서의 공식 등급이 아니라, 지금까지 확보·분석한 현장자료를 바탕으로 한 **우리 내부 기술평가**다.

## 2. 난이도 평가

### 우리 내부 기술평가

**종합 난이도: 약 8.5~9 / 10**

이 평가는 AI 알고리즘 자체보다 다음 복합 난이도를 포함한다.

- PLC/XG5000 원본과 실제 운전본 식별
- HMI/SCADA Tag·Address·Topic·Alarm·Trend 계보 추적
- Historian 메타 DB와 실제 History Storage 분리구조 해석
- 장기간 공정 시계열 데이터 확보
- 정수/하수 서로 다른 공정 모델 개발
- 운전 Mode·Set Point·RUN/STOP·Fault·Interlock 등 제어문맥 정합화
- 설비 전력·수질·운전조건의 동시 최적화
- AI 권고값을 SCADA/PLC 안전조건 아래에서 적용하는 연계구조

즉, 이 프로젝트는 **AI 모델 개발 + OT 시스템 리버스 엔지니어링 + 데이터 엔지니어링 + 제어/최적화 + 현장 검증**이 동시에 필요한 과제다.

## 3. 왜 어려운가

### 중랑 하수

현재까지 확인된 구조는 다음과 같다.

```text
PLC / 현장설비
→ CIMON SCADA
→ SQL / Logger / DB
→ AI 서버
→ SCADA
→ A2 PILOT PLC 안전·운전 Rule
→ 실제 설비
```

중랑에서는 단순히 DB 컬럼만 보는 것으로 충분하지 않다. **PLC 주소 → SCADA Tag → DB 저장값 → 실제 공정 의미 → 운전 Rule**을 역추적해야 한다.

특히 A2O PLC와 A2 PILOT PLC의 역할이 다르며, A2 PILOT의 실제 제어 Rule은 원본 프로그램과 운전조건을 확인하기 전에는 확정할 수 없다.

### 광암 정수

현재까지 규명한 광암 주요 계보는 다음과 같다.

```text
XG5000 PLC
→ DeviceXPlorer / GFENet / SuiteLink 계층
→ Wonderware InTouch
→ Wonderware Historian Runtime
→ History SQL 조회 인터페이스
→ History Storage
→ AI 학습데이터
```

광암에서는 처음에 대용량 `WWALMDB`가 장기 공정이력 DB처럼 보일 수 있었으나, 분석 결과 **Alarm/Event DB**로 분리됐다.

또한 `Runtime.bak`은 Historian의 Tag·Storage 메타정보와 History 조회 인터페이스를 포함하지만, 장기간 공정값 본체가 일반 SQL Table Row 형태로 전부 들어 있는 구조는 아니었다. POS11에서 `Runtime.dbo.History` 실조회와 `StorageLocation` 확인까지 진행하여 실제 History Storage 구조를 별도로 확인했다.

이 과정은 일반적인 AI 데이터 전처리보다 **산업 시스템 데이터계보 역추적** 성격이 강하다.

## 4. 영역별 난이도

| 영역 | 내부 난이도 | 현재 판단 이유 |
|---|---:|---|
| PLC / XG5000 분석 | 높음 | 백업·구버전·현행본 구분, 주소·Symbol·Program Block 추적 필요 |
| HMI / InTouch 분석 | 높음 | Tag, AccessName, Topic, Item, Alarm, Trend, 화면 연결 필요 |
| Historian 분석 | 매우 높음 | SQL Runtime 메타계층과 실제 History Storage가 분리됨 |
| 데이터 정합화 | 매우 높음 | 시간축, 결측, 운전 Mode, 상태변수, Set Point 문맥까지 맞춰야 함 |
| AI 예측 | 중~높음 | 데이터 기반이 정리된 뒤에는 상대적으로 정형화 가능 |
| 설비 전력 예측 | 높음 | RUN/Hz/가동대수/유량/공정조건과 전력을 함께 해석해야 함 |
| 제어·최적화 / MPC | 매우 높음 | 수질·설비·PLC 제약조건을 동시에 만족해야 함 |
| SCADA / PLC 연계 | 매우 높음 | 실제 설비 적용이므로 안전조건·권한·Mode·Fallback 필수 |
| 정수/하수 동시 대응 | 매우 높음 | 공정과 주요 변수가 달라 동일 AI 모델을 그대로 사용할 수 없음 |
| 현장·문서·기관 조율 | 높음 | 자료가 분산되고 시점·버전·책임경계를 계속 확인해야 함 |

## 5. 정수와 하수는 사실상 별도 개발축

### 중랑 하수 개발축

```text
유입량
→ DO / MLSS / ORP / NH4-N / NO3-N 등 공정상태
→ 송풍기 / 내부반송 / 유량조정펌프
→ 설비 전력
→ 방류수질 / 공정 안정성
→ 최적 Hz / Set Point
```

우선 대상은 송풍기 M203, 내부반송 M204, 유량조정펌프 M102 순으로 검토한다.

### 광암 정수 개발축

```text
원수량 / 정수량
→ 탁도 및 주요 수질
→ PAC / CO2 / H2O2 / 염소 등 약품
→ 펌프 RUN / Hz / 가동대수
→ 설비 전력
→ 정수 품질
→ 최적 운전조건 / Set Point
```

광암은 **정수 AI 학습·검증 기준 시설**이며, 개발·검증·표준화 후 명장 최종 적용을 목표로 한다.

따라서 “AI 프로젝트 하나를 두 시설에 복사”하는 구조가 아니라, **공통 데이터·연계 아키텍처를 공유하되 정수와 하수의 예측·최적화 모델은 별도로 개발**하는 것이 원칙이다.

## 6. 현재까지 넘어온 단계

### 시작 시점

```text
[현장 시스템 구조 불명확]
[실제 데이터 저장위치 불명확]
[PLC / HMI / DB 역할 혼재]
```

### 현재 확인된 상태

```text
[PLC/HMI/SCADA/Historian 구조 상당부분 규명]
        ↓
[광암 POS11 History 실조회 확인]
        ↓
[광암 Historian 실제 Storage 경로 확인]
        ↓
[중랑 PLC / SCADA / DB 역할 구분 진행]
        ↓
[AI 학습데이터 확보 및 Master DB 설계 단계로 이동]
```

광암에서는 다음이 직접 확인됐다.

- POS11 `Runtime.dbo.History`에서 실제 공정 시계열 값 조회 가능
- `WWALMDB`는 Alarm/Event DB
- `Runtime`은 Historian Tag/Storage 메타 + History 조회 인터페이스
- 실제 Storage 경로:
  - `D:\Historian\Data\Circular`
  - `R:\Overflow\Data`
  - `D:\Historian\Data\Buffer`
  - `D:\Historian\Data\Permanent`

다만 **장기간 AI 학습용 정식 데이터셋은 아직 우리 측에 인수 완료되지 않았다.**

## 7. 앞으로의 개발 위치

```text
현재
[구조분석 및 데이터계보 상당부분 확보]
        ↓
1. 장기간 실제 데이터 확보
        ↓
2. 데이터 사전 / Master DB 구축
        ↓
3. 데이터 품질진단 / Baseline 분석
        ↓
4. 유입·수요 예측
        ↓
5. 수질·공정상태 예측
        ↓
6. 설비 전력 예측
        ↓
7. 제약조건 기반 제어·최적화
        ↓
8. Shadow Mode
        ↓
9. 운영자 승인형 적용
        ↓
10. 제한적 자동제어 검토
```

AI 모델 개발은 이 전체 흐름의 중간 단계다. **데이터계보와 운전문맥이 틀리면 이후 모델 정확도가 높아도 현장 적용에는 사용할 수 없다.**

## 8. 현재 가장 큰 3개 리스크

### 1) 광암 Historian 장기 실제값 확보

**현재 확인됨**
- Historian 존재
- POS11 History 조회 가능
- 실제 Storage Path 확인

**아직 부족함**
- 각 Storage 총용량·파일수
- 실제 보존기간
- AI용 장기간 데이터 인수

**우리가 해야 할 일**
- Storage 용량 확인
- 용량 가능 시 원본 Storage 확보
- 최신 Runtime BAK / Tag 메타 함께 보존
- 최종 AI 학습본은 History Query Export로 표준화

### 2) 중랑 A2 PILOT 실제 제어 Rule 확정

**현재 확인됨**
- A2O PLC와 A2 PILOT PLC 역할이 다름
- A2 PILOT를 공정제어 PLC로 봐야 함

**추가 확인 필요**
- RUN/STOP
- AUTO/MANUAL
- Interlock / Run Permit
- Timer / Delay
- 최소·최대 Hz
- 교번·고장대체 운전
- DO/MLSS/ORP 등 조건에 따른 실제 Rule

**원칙**
- 원본/현장 근거 없이 실제 제어 Rule을 확정하지 않는다.

### 3) AI 대상 Tag와 운전조건의 Master DB 정리

단순 센서값만 저장하면 부족하다.

동일 시간축에 최소한 다음을 함께 저장해야 한다.

```text
Timestamp
수질 / 유량 / 수량
RUN / STOP
운전 Hz
가동대수
설비별 kW
AUTO / MANUAL
Interlock
Run Permit
Alarm / Fault
Set Point
실제 적용값
제어사유
```

AI 적용 이후에는 추가로:

```text
AI 권고값
승인 / 거부
거부사유
모델 버전
AI 명령 ID
```

을 관리한다.

## 9. 프로젝트에서 특히 위험한 오해

가장 큰 관리 리스크는 이 과제가 겉으로는 **“AI 모델 하나 만들면 되는 일”**처럼 보이는 것이다.

실제 구현에는 다음이 모두 필요하다.

```text
OT 구조분석
+ PLC / HMI / SCADA 통신분석
+ Historian / DB 분석
+ 데이터 엔지니어링
+ AI 예측
+ 설비 전력 모델링
+ 제어·최적화
+ 안전조건
+ 현장 검증
```

따라서 일정과 견적도 **AI 모델 코딩 시간만으로 산정하면 안 된다.** 데이터 확보·역추적·현장확인·연계검증 시간이 별도 작업으로 관리돼야 한다.

## 10. 확정 / 추정 / 추가확인 / 개발방향 구분

### 자료로 확인된 내용

- 중랑과 광암의 현장 시스템 구조가 서로 다름
- 광암 InTouch / DeviceXPlorer / Historian 구성 존재
- 광암 POS11 `Runtime.dbo.History` 실제 조회 가능
- 광암 Historian StorageLocation 실제 경로 4개 확인
- WWALMDB는 Historian 장기 공정값 DB가 아니라 Alarm/Event DB
- 정수와 하수는 같은 AI 모델을 그대로 사용할 수 없는 공정구조

### 합리적 추정 / 내부 평가

- 종합 난이도 **8.5~9/10**
- 이 과제를 산업 OT·데이터·AI 통합 프로젝트로 분류
- 정수/하수 개발축을 사실상 별도 AI 개발축으로 관리하는 것이 적절

### 추가 확인 필요

- 광암 History Storage 실제 총용량과 보존기간
- `R:` Storage의 물리적 정체
- 중랑 A2 PILOT 상세 운전 Rule
- 각 AI 대상 Tag의 최종 데이터 품질·결측률·저장주기
- 현장 자동제어 허용범위

### 우리가 제안하는 개발 방향

```text
구조분석
→ 데이터 사전
→ 운전 Rule
→ 데이터 품질진단
→ Master DB
→ Baseline
→ 예측모델
→ 전력예측
→ 제어·최적화
→ Shadow Mode
→ 운영자 승인형
→ 제한적 자동제어
```

AI는 PLC를 대체하거나 우회하지 않고, **더 좋은 운전 Set Point를 계산하는 상위 최적화 계층**으로 둔다.

---

## 관련 문서

- [[../00-광암-홈|광암 홈]]
- [[../20-현장-시스템/01-광암-데이터흐름-및-통신구조]]
- [[../30-데이터-분석/06-광암-Historian-Runtime-DB-분석-및-AI학습데이터-확보판정]]
- [[../30-데이터-분석/07-광암-Historian-현장-원본확보-절차]]
- [[../30-데이터-분석/08-광암-Historian-AI학습데이터-추출전략]]
