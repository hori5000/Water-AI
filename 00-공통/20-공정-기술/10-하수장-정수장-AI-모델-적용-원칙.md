---
type: technical-guide
project: Water AI 공통
scope: 하수장·정수장
status: active
tags:
  - water-ai
  - wastewater-ai
  - optimization
  - mpc
  - llm
  - rag
---

# 하수장·정수장 AI 모델 적용 원칙

> [!summary] 결론
> 하수장·정수장 **실제 운전 예측·최적화에는 LLM보다 전용 수치모델이 주력**이다.  
> **XGBoost/LightGBM → 필요한 항목만 시계열 딥러닝 → MPC/제약조건 최적화 → 안전 Rule → 현장 실증** 순으로 가고, **LLM/RAG는 옆에서 설명·검색·보고를 지원**한다.

---

## 1. 전체 역할 분리

```mermaid
flowchart TB
    subgraph CORE[실제 공정 운전의 주력 영역]
      P[공정 상태 예측<br/>XGBoost · LightGBM<br/>LSTM · TCN 등]
      O[운전 최적화<br/>MPC · Bayesian Optimization<br/>제약조건 최적화]
      A[이상탐지<br/>Isolation Forest<br/>Autoencoder · SPC]
      S[안전 검증<br/>Rule Engine<br/>AUTO · Fault · Interlock<br/>Min/Max]
    end

    subgraph LLM[LLM / RAG 보조 영역]
      Q[운전원 Q&A]
      M[매뉴얼·기술자료 검색]
      E[예측·최적화 결과 설명]
      R[장애 원인 정리·보고서 작성]
    end

    P --> O --> S
    A --> S
    Q -.-> S
    M -.-> E
    P -. 결과 .-> E
    O -. 결과 .-> E
    E -. 설명 .-> R
```

### 핵심 경계

| 구분 | 담당 | 비고 |
|---|---|---|
| 수질·유량·전력 수치 예측 | 전용 ML/시계열 모델 | LLM 주력 사용 금지 |
| Set Point 후보 계산 | MPC/최적화 엔진 | 공정 제약조건 반영 |
| 이상 징후 탐지 | 이상탐지 모델 | 센서·설비·공정 이상 |
| 최종 안전 확인 | Rule Engine / PLC 안전로직 | AI가 우회하면 안 됨 |
| 설명·검색·Q&A·보고 | LLM/RAG | 운전원 의사결정 지원 |

---

## 2. 목적별로 어떤 기법을 쓰나

```mermaid
flowchart LR
    X[목적] --> P[공정 예측]
    X --> A[이상탐지]
    X --> O[운전 최적화]
    X --> E[에너지 최적화]
    X --> V[변수선정·설명]
    X --> L[LLM 보조]

    P --> P1[XGBoost / LightGBM<br/>1차 Baseline]
    P --> P2[Random Forest]
    P --> P3[LSTM / GRU / TCN<br/>필요 시 고도화]
    P --> P4[시계열 Transformer<br/>데이터 충분할 때]

    A --> A1[Isolation Forest]
    A --> A2[Autoencoder]
    A --> A3[One-Class SVM]
    A --> A4[SPC / 통계 임계관리]

    O --> O1[MPC]
    O --> O2[제약조건 기반 최적화]
    O --> O3[Bayesian Optimization]
    O --> O4[강화학습 RL<br/>후순위]

    E --> E1[회귀 / 부스팅]
    E --> E2[설비 전력 예측]
    E --> E3[부하배분·운전스케줄 최적화]

    V --> V1[SHAP]
    V --> V2[상관분석]
    V --> V3[PCA]

    L --> L1[RAG / 운전원 Q&A]
    L --> L2[장애 원인 설명]
    L --> L3[보고서·결과 해석]
```

> [!tip] 1차 모델 추천
> 처음부터 딥러닝으로 가지 말고 **LightGBM/XGBoost로 Baseline을 먼저 만든다.**  
> Baseline으로 부족한 예측 항목에만 LSTM/TCN/Transformer 계열을 추가한다.

---

## 3. 하수장과 정수장의 입력·출력 예시

```mermaid
flowchart TB
    subgraph WW[하수장]
      WWI[유입량 · DO · MLSS · pH · ORP<br/>송풍량 · 펌프 Hz · 전력량] --> WWP[예측]
      WWP --> WWO[송풍량 / 펌프 / 운전조건<br/>Set Point 후보]
    end

    subgraph WT[정수장]
      WTI[원수 유량 · 탁도 · pH · 수온<br/>잔류염소 · 약품주입 · 전력량] --> WTP[예측]
      WTP --> WTO[약품주입 / 펌프 / 여과·송수<br/>Set Point 후보]
    end

    WWO --> SAFE[안전 Rule + 운영자 승인]
    WTO --> SAFE
    SAFE --> FIELD[SCADA / PLC / 실제 설비]
```

### 하수장 주요 목표 예

- 방류 수질 유지
- DO 등 공정 상태 안정화
- 송풍기·펌프 전력 최소화
- 이상상태 조기 탐지
- 운전조건 변경 시 향후 상태 예측

### 정수장 주요 목표 예

- 탁도·잔류염소 등 수질 기준 유지
- 약품 주입량 최적화
- 펌프·송수·여과 에너지 절감
- 원수 변화에 따른 선제 운전
- 설비 이상·센서 이상 조기 탐지

---

## 4. 권장 시스템 구조

```mermaid
flowchart LR
    F[센서 / 설비] --> PLC[PLC / SCADA]
    PLC --> DB[Logger / 운영 DB]
    DB --> PRE[예측 모델<br/>10~60분 상태 예측]
    PRE --> OPT[최적화 엔진<br/>Set Point 후보]
    OPT --> RULE[Rule Engine<br/>AUTO / FAULT / INTERLOCK<br/>상·하한 / 허용운전조건]
    RULE --> OP[운영자 판단<br/>또는 승인형 제어]
    OP --> PLC2[PLC 안전로직]
    PLC2 --> ACT[실제 설비]

    LLM[LLM / RAG] -. 매뉴얼 검색 .-> OP
    LLM -. 결과 설명 .-> PRE
    LLM -. 최적화 이유 설명 .-> OPT
    LLM -. 보고서 / Q&A .-> OP
```

> [!danger] 금지선
> - LLM이 PLC 값을 직접 산출하여 즉시 쓰는 구조
> - LLM이 Interlock/Permit/Min-Max Rule을 우회하는 구조
> - 검증되지 않은 모델을 처음부터 자동제어에 연결하는 구조

---

## 5. 실제 구축 순서

```mermaid
flowchart LR
    S1[1. 데이터 정비<br/>태그 정합성<br/>시간축 정렬<br/>결측·이상치] -->
    S2[2. Baseline<br/>LightGBM / XGBoost] -->
    S3[3. 시계열 고도화<br/>필요 항목만<br/>LSTM / TCN 등] -->
    S4[4. 최적화 / MPC<br/>제약조건 반영] -->
    S5[5. Shadow Mode<br/>실제 제어 없이 비교] -->
    S6[6. 승인형 제어<br/>운영자 승인] -->
    S7[7. 제한 자동<br/>검증된 범위만]
```

### 선행조건

```mermaid
flowchart TB
    D[데이터 표준화] --> M[모델 개발]
    R[Rule Book 확정<br/>AUTO/MANUAL · Fault · Interlock<br/>Permit · Min/Max] --> M
    B[성능 기준 확정<br/>수질 · 안정성 · 전력 · 예측정확도] --> M
    M --> V[검증 / 실증]
```

---

## 6. LLM은 어디에 붙이나

```mermaid
flowchart LR
    DOC[매뉴얼 · 설계서 · WBS · 시험자료] --> RAG[RAG]
    RAG --> LLM[LLM]
    AI[예측·최적화 결과] --> LLM
    ALARM[알람 · 장애 · 로그] --> LLM
    LLM --> Q[운전원 Q&A]
    LLM --> WHY[왜 이 Set Point인가 설명]
    LLM --> REP[보고서 / 장애 원인 정리]
    LLM --> GUIDE[점검 절차 안내]
```

### 예시

**질문**  
`왜 지금 송풍량을 줄이라고 했어?`

**LLM이 설명해야 할 내용**  
- 최근 DO 추세
- 유입부하 변화
- 예측모델의 10~60분 전망
- 최적화 모델이 계산한 전력 절감 효과
- Rule Engine에서 확인한 안전범위
- 최종적으로 운전자가 판단해야 할 항목

즉, **LLM이 값을 만든 것이 아니라 이미 계산된 근거를 사람이 이해하기 쉽게 설명**하는 구조다.

---

## 7. 중랑 적용 시 현재 권장 형태

```mermaid
flowchart LR
    S[센서 / 설비] --> A2O[A2O PLC<br/>수집 중심]
    A2O --> SCADA[CIMON SCADA<br/>태그 허브]
    SCADA --> DB[Logger / SQL<br/>AI Master DB]
    DB --> AI[예측 모델<br/>LightGBM/XGBoost → 시계열]
    AI --> MPC[MPC / 최적화<br/>권고 Set Point]
    MPC --> RULE[안전 Rule 검증]
    RULE --> PILOT[A2 PILOT PLC<br/>안전조건 확인]
    PILOT --> ACT[실제 설비]

    L[LLM / RAG] -. 설명 · 검색 · 보고 .-> AI
    L -. 운전원 Q&A .-> RULE
```

> [!warning] 중랑 현장 확인 필요
> A2 PILOT PLC의 실제 RUN/STOP, AUTO/MANUAL, Interlock, Permit, 최소/최대 Hz 등 **제어 Rule을 확정한 뒤** AI 권고값을 연결한다.

---

## 8. 기술 선택 우선순위

**권장 순서**

`XGBoost / LightGBM → 시계열 모델 → MPC/최적화 → 이상탐지 → Shadow 실증 → 승인형/제한자동 → LLM/RAG 통합`

### 판단 기준

| 단계 | 먼저 확인할 것 | 다음 단계로 가는 조건 |
|---|---|---|
| 데이터 | 시간축·태그·단위·결측 | 학습 가능한 품질 확보 |
| Baseline | 예측 정확도·재현성 | 단순모델 한계 확인 |
| 시계열 | 시간 의존성 개선 효과 | Baseline 대비 실질 개선 |
| 최적화 | 수질·공정 제약 준수 | 안전 범위 안에서 절감효과 |
| Shadow | 실제 운전과 비교 | 충분한 기간 안정성 확인 |
| 자동화 | 운영자 승인·안전 Rule | 제한된 범위부터 단계 확대 |
| LLM/RAG | 근거 추적·문서 정확성 | 설명·지원 용도로만 적용 |

---

## 9. 최종 원칙

```mermaid
flowchart LR
    A[전용 예측 모델] --> B[최적화 엔진]
    B --> C[안전 Rule]
    C --> D[PLC / 현장]
    L[LLM / RAG] -. 설명 · 검색 · 보고 .-> A
    L -. 의사결정 지원 .-> D
```

> [!important] 한 문장 정리
> **공정을 움직이는 것은 수치 예측·최적화·안전검증 체계이고, LLM은 그 옆에서 사람에게 이유를 설명하고 자료를 찾아주는 역할이다.**

[[00-공정-AI-기술-홈|← 공정/AI 기술 홈]] · [[../00-Water-AI-공통-홈|Water AI 공통 홈]]
